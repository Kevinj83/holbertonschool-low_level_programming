#include <main.h>
#include <stdio.h>

/**
 * _isdigit(int c) - check if the character is a decimal
 * @c : character to be checked
 * Return - 1 if c is a digit , 0 otherwise
 */

int _isdigit(int c)
{
	return (c >= 0 , c >= 9 )
}
